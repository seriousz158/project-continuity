interface:
  display_name: "Project Continuity"
  short_description: "Safe project handoffs with receipt capacity governance"
  default_prompt: "Use $project-continuity to inspect, resume, update, save, compact, or verify this project's handoff safely."
